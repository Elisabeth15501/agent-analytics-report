[pytest]
# 测试根目录：仅收集 tests/ 下的用例
testpaths = tests
# -ra          : 显示 summary（passed/failed/skipped 概要）
# --alluredir  : 生成 Allure 标准结果目录（allure-results/），供 allure serve / 自渲染使用
addopts = -ra --alluredir=allure-results
# 复用 conftest.py 中已注册的 marker 体系（smoke/integration/regression/golden/...）
